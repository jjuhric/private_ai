{
  "name": "private-ai-assistant",
  "version": "2.3.0",
  "description": "Local and Online AI Assistant with multi-agent orchestration and tools",
  "main": "backend/server.js",
  "scripts": {
    "install:all": "npm install && npm audit fix --force && npm install --prefix backend && npm audit fix --force --prefix backend && npm install --prefix frontend --legacy-peer-deps && npm audit fix --force --prefix frontend --legacy-peer-deps",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test": "npm run test:backend && npm run test:frontend",
    "test:e2e": "playwright test",
    "coverage:backend": "npm run test:coverage --prefix backend",
    "coverage:frontend": "npm run test:coverage --prefix frontend",
    "coverage": "npm run coverage:backend && npm run coverage:frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "google-news-url-decoder": "^1.2.2",
    "node-html-parser": "^8.0.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1"
  },
  "engines": {
    "node": ">=25.5.0"
  }
}
